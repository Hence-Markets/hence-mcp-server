{
  "name": "@hence-markets/mcp-server",
  "version": "0.1.0",
  "description": "Hence Market MCP server for safe agentic market workflows.",
  "type": "module",
  "license": "UNLICENSED",
  "bin": {
    "hence-mcp": "./dist/index.js",
    "hence-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "skills"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js --http",
    "start:stdio": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node test/protocol-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}

